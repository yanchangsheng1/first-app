# 🦢 Pixel Goose 3D

像素游戏风格的白色大鹅 3D 展示，基于 **Vue 3 + Vite + Three.js**。

一只用体素方块拼出的白色大鹅，配合像素化后处理（`RenderPixelatedPass`）呈现复古游戏质感，支持鼠标拖拽自由查看。

## 特性

- 🦢 **Q 版体素大鹅**：参考实拍像素图，纯代码用 `BoxGeometry` 拼装的白色大鹅——大圆头、粗黑像素描边、黑豆眼 + 白高光、粉脸蛋、宽黄扁嘴、张开的翅膀、黄脚蹼，身体带粉/蓝/紫柔和渐变点缀。
- 🎮 **像素游戏风格**：使用 `RenderPixelatedPass` 做像素化后处理，棋盘格地面、漂浮的柔彩方块，营造复古游戏氛围。
- 🎬 **GSAP 过渡动画**：相机切换、配色/材质变化、面板入场全部用 GSAP 平滑过渡。
- 🧩 **部件编辑器（左侧栏目）**：把大鹅拆成可编辑图层（羽毛/嘴巴/脚蹼/腮红/翅尖/眼睛/斑点/描边），点击列表或直接点击模型即可选中。
- 🎨 **属性面板（右侧）**：对选中部件调整 **颜色**、**材质质感**（哑光/亮面/金属）、**描边粗细与显隐**，以及像素颗粒、自动旋转。
- 🎥 **视角切换（顶部）**：**2D / 3D** 以及 **正面 / 侧面 / 底面 / 透视** 相机预设。
- 🌈 **预设配色搭配（中间底部）**：经典奶白、蜜桃粉、薄荷奶绿、天空蓝、薰衣草、暗夜霓虹，一键换肤。
- 🖱 **鼠标交互**：拖拽旋转、滚轮缩放、右键平移（`OrbitControls`），点击拾取部件。

## 快速开始

```bash
npm install
npm run dev      # 启动开发服务器 http://localhost:5173
```

## 构建

```bash
npm run build    # 产物输出到 dist/
npm run preview  # 预览构建产物
```

## 目录结构

```
├── index.html
├── vite.config.js
├── package.json
└── src
    ├── main.js
    ├── App.vue                 # 界面与 HUD 控制面板
    ├── styles/global.css       # 全局像素风样式
    └── three
        ├── PixelGooseScene.js  # 场景/相机/灯光/后处理/GSAP过渡/射线拾取/动画循环
        └── buildGoose.js       # Q 版体素大鹅模型（按图层拆分可编辑材质）
```

## 界面布局

- **顶部**：2D/3D 切换 + 正面/侧面/底面/透视 相机预设。
- **左侧**：部件图层列表，点击选中要编辑的部件。
- **右侧**：选中部件的属性（颜色 / 材质质感 / 描边 / 像素颗粒 / 自动旋转）。
- **中间底部**：预设配色搭配，一键切换整体配色。

## 操作说明

| 操作 | 效果 |
| --- | --- |
| 鼠标左键拖拽 | 旋转视角（3D） |
| 滚轮 | 缩放 |
| 鼠标右键拖拽 | 平移 |
| 单击模型 | 选中对应部件 |
| 左侧列表 | 选中部件图层 |
| 右侧取色器 | 修改选中部件颜色 |
| 底部配色 | 一键套用预设配色 |
